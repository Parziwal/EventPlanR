include: package:very_good_analysis/analysis_options.5.1.0.yaml
analyzer:
  exclude: [lib/**.config.dart, lib/**.g.dart]
  errors:
    invalid_annotation_target: ignore
linter:
  rules:
    public_member_api_docs: false

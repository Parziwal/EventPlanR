version: '3.4'

services:
  event-planr-event-db:
    image: postgis/postgis
    environment:
      - POSTGRES_DATABASE=event_dev
      - POSTGRES_USER=event_dev
      - POSTGRES_PASSWORD=event_dev
    ports:
      - 5432:5432
    volumes:
      - event-planr-event-db-data:/var/lib/postgresql

volumes:
  event-planr-event-db-data:
